# 02 · 学习笔记（你写，助手批注）

> 用法：按学习清单逐条写你的理解（不用长，讲得对就行），
> 实验输出直接贴进来。写完喊助手验收。

## 学习清单答案
### 1. 大白话解释"下一个 token 预测"
（待填）

### 2. 注意力 O(S²) 的含义，seq=2048 的具体计算
（待填）

### 3. GQA 省了多少（每层 KV 参数 / 推理 KV cache）
（待填）

### 4. RoPE 旋转怎么产生相对位置
（待填）

### 5. QK-Norm 不加会怎样
（待填）

### 6. SwiGLU 为什么 3 个矩阵
（待填）

### 7. tie embedding 是什么、为什么小模型要开
（待填）

### 8. 0.5B 参数量手算过程
（待填）

### 9. 采样管线步骤与作用
（待填）

## 实验记录
> 2026-09-25 助手在 node05 代跑 E1/E2/E3（当时两张 4090 被 SGLang 占满，E1 走 CPU fp32）。
> 输出已贴好。你的任务：逐行对照"学习清单"验证，确认理解后把清单 9 条答案写上来。

### E1 前向走查（输出粘贴处）
```
[E1] 句子: 我们是一个两人小组，从 0 开始训练一个 0.5B 的小智能体。
[E1] 词表=6400 层数=24 hidden=1280 heads=16/8 head_dim=80 ffn=4032
[E1] S=22  device=cpu  dtype=torch.float32
  input_ids      (1, 22)  (整数 0~6399)
  Embedding      -> h (1, 22, 1280)
  layer 0 out    (1, 22, 1280)   mean=+0.0030 std=0.2025
  ...（layer1~23 同形状，std 从 0.33 逐层缓升到 1.53，残差累加的正常表现）...
  layer23 out    (1, 22, 1280)   mean=+0.0321 std=1.5298
  final RMSNorm  -> h (1, 22, 1280)
  lm_head        -> logits (1, 22, 6400)   min=-2.06 max=2.33 |mean|=0.4557
  初始 loss = 8.7316    均匀分布参考 ln(6400) = 8.7641
[E1] PASS
```
（完整 24 层明细见 experiments/ 重跑输出，逐层形状均为 (1, 22, 1280)，与 fig2 一致）

### E2 参数审计（输出粘贴处）
```
单层拆解（× 24 层）:
  gate_proj/up_proj/down_proj   各 5.161M（FFN 合计 15.483M，占每层 75.89%）
  q_proj/o_proj                 各 1.638M
  k_proj/v_proj                 各 0.819M（GQA 16:8，KV 减半）
  norm 合计                     0.003M
  每层合计     20.401M
24 层合计      489.619M
Embedding      8.192M  (lm_head tied，不重复计)
>>> 总计        497.812M = 497,812,480 个参数
手算总计 ≈ 497.7M（README 给 ~498M）  实测 vs 手算 偏差 = 0.01%  PASS
```

### E3 手写迷你 GPT（代码路径 + 结果）
- 代码：`experiments/E3_mini_gpt.py`（100,416 参数，自包含只要 torch）
- 结果：loss 0.69 → 0.0000（50 步内）；输入 `a b a b` → 预测 `b a b` ✓ PASS
- 待你回答：causal mask / 残差 / norm 位置 各在代码哪一行、起什么作用

### E4 注意力可视化（选做）
（待填）

## 踩坑与疑问
（待填）

## 助手批注
（验收后填写）
